[project]
name = "engineerview"
version = "0.1.0"
description = "EngineerView cockpit service"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.23",
  "httpx>=0.25",
]

[project.scripts]
engineerview = "engineerview.main:app"
