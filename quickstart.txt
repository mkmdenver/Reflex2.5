cd <unzipped-folder>
python -m venv .venv
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
pip install -r DataHub\requirements.txt
pip install -r Evaluator\requirements.txt

Copy-Item .env.example .env

# Optional: switch to Polygon live
# setx POLYGON_API_KEY "<your key>"
# setx POLYGON_ENABLE_WS "true"

scripts\run_all
